menu "KernelSU - SUSFS"

config KSU_SUSFS
	bool "KernelSU addon - SUSFS"
	depends on KSU
	default y

config KSU_SUSFS_HAS_MAGIC_MOUNT
	bool "Magic mount support"
	depends on KSU
	default y

config KSU_SUSFS_SUS_PATH
	bool "Hide suspicious path"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_SUS_MOUNT
	bool "Hide suspicious mounts"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT
	bool "Auto-hide KSU default mounts"
	depends on KSU_SUSFS_SUS_MOUNT
	default y

config KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT
	bool "Auto-hide bind mounts"
	depends on KSU_SUSFS_SUS_MOUNT
	default y

config KSU_SUSFS_SUS_KSTAT
	bool "Spoof kstat"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_SUS_OVERLAYFS
	bool "Spoof overlayfs kstat"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_TRY_UMOUNT
	bool "Try umount"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT
	bool "Auto try-umount bind mounts"
	depends on KSU_SUSFS_TRY_UMOUNT
	default y

config KSU_SUSFS_SPOOF_UNAME
	bool "Spoof uname"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_ENABLE_LOG
	bool "Enable logging"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS
	bool "Hide kallsyms symbols"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
	bool "Spoof cmdline/bootconfig"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_OPEN_REDIRECT
	bool "Open redirect"
	depends on KSU_SUSFS
	default y

# --- GKI Backport features (4.14 enhanced) ---
config KSU_SUSFS_SUS_MAP
	bool "Hide suspicious maps entries"
	depends on KSU_SUSFS
	default y
	help
	  Allow hiding/spoofing entries in /proc/self/maps output.
	  Support spoofing inode, device, pathname, pgoff, and protection flags
	  for individual memory mappings. Backported from SUSFS GKI branch.

config KSU_SUSFS_SUS_MEMFD
	bool "Hide suspicious memfd"
	depends on KSU_SUSFS
	default y
	help
	  Block creation of memfd with specific names matching user-defined patterns.
	  Backported from SUSFS GKI branch.

config KSU_SUSFS_SUS_PROC_FD_LINK
	bool "Hide suspicious /proc/fd link"
	depends on KSU_SUSFS
	default y
	help
	  Spoof readlink results for /proc/self/fd/ entries.
	  Backported from SUSFS GKI branch.

endmenu
